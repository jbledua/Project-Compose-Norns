# --- Tailscale ---
TS_HOSTNAME=norns
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxx

# --- Stack name (used for named volumes) ---
STACK_NAME=norns

# --- Host paths (bind mounts) ---
# If using a NAS, mount it on the host first, then point these at it.
MODELS_DIR=/srv/ai/models
INPUT_DIR=/srv/ai/input
OUTPUT_DIR=/srv/ai/output

# --- LAN exposure (optional) ---
# Bind IP for published ComfyUI port:
#  - 0.0.0.0 exposes on all interfaces (LAN)
#  - 127.0.0.1 limits to localhost only
COMFYUI_HOST_BIND=0.0.0.0

# Host port to publish ComfyUI (internal default is 8188)
COMFYUI_PORT=8188

# --- AI-Dock ComfyUI settings ---
# ComfyUI interface port as used by ai-dock supervisor (default 8188)
COMFYUI_PORT_HOST=8188

# Optional: token for gated downloads from Civitai
CIVITAI_TOKEN=replace-with-your-token-if-needed